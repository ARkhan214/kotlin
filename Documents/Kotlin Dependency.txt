
## 🔹 1️⃣ `androidx-core-ktx`

কাজ কী করে:

 Android core API গুলোকে Kotlin-friendly বানায়
 Boilerplate code কমায়

কখন লাগে:

 Kotlin Android project হলে প্রায় সব সময়ই লাগে

---

## 🔹 2️⃣ `androidx-core-splashscreen`

কাজ কী করে:

 Android 12+ এর official Splash Screen API
 App launch এর সময় সুন্দর splash দেখায়

কখন লাগে:

 App start-up UX improve করতে

---

## 🔹 3️⃣ `androidx-datastore-preferences`

কাজ কী করে:

 SharedPreferences-এর modern replacement
 Async + Coroutine based key-value storage

কখন লাগে:

 Token, settings, dark mode, login state

---

## 🔹 4️⃣ `androidx-fragment-ktx`

কাজ কী করে:

 Fragment ব্যবহারে Kotlin extension দেয়
 Fragment transaction সহজ করে

কখন লাগে:

 Fragment-based UI বানালে

---

## 🔹 5️⃣ `androidx-lifecycle-livedata-ktx`

কাজ কী করে:

 LiveData + Coroutine support
 Data change হলে UI auto update

কখন লাগে:

 MVVM architecture ব্যবহার করলে

---

## 🔹 6️⃣ `androidx-lifecycle-viewmodel-ktx`

কাজ কী করে:

 ViewModel এর Kotlin version
 UI state ধরে রাখে (rotation safe)

কখন লাগে:

 MVVM architecture এ must-have

---

## 🔹 7️⃣ `androidx-security-identity-credential`

কাজ কী করে:

 Digital Identity / Credential manage করে
 Secure ID storage (advanced)

কখন লাগে:

 Wallet / Gov ID / Secure identity app

---

## 🔹 8️⃣ `app-update`

কাজ কী করে:

 Google Play In-App Update feature
 App এর ভিতরেই update prompt দেখায়

কখন লাগে:

 Force / Flexible update দরকার হলে

---

## 🔹 9️⃣ `app-update-ktx`

কাজ কী করে:

 In-App Update এর Kotlin extension
 Code short & clean

কখন লাগে:

 Kotlin project হলে `app-update` এর সাথে

---

## 🔹 🔟 `junit`

কাজ কী করে:

 Unit testing framework
 Pure Java/Kotlin logic test

কখন লাগে:

 Business logic test করতে

---

## 🔹 1️⃣1️⃣ `androidx-junit`

কাজ কী করে:

 Android Instrumentation test
 Context, Activity test করা যায়

কখন লাগে:

 Android-specific testing

---

## 🔹 1️⃣2️⃣ `androidx-espresso-core`

কাজ কী করে:

 UI automation testing
 Button click, text input test

কখন লাগে:

 UI behavior test করতে

---

## 🔹 1️⃣3️⃣ `androidx-appcompat`

কাজ কী করে:

 Backward compatibility দেয়
 পুরোনো Android version support

কখন লাগে:

 প্রায় সব Android project এ লাগে

---

## 🔹 1️⃣4️⃣ `material`

কাজ কী করে:

 Google Material Design UI components

Example:

 Button, Snackbar, BottomNavigation

কখন লাগে:

 Modern UI বানাতে

---

## 🔹 1️⃣5️⃣ `androidx-activity`

কাজ কী করে:

 Activity lifecycle manage
 New Activity Result API

কখন লাগে:

 Every Android app

---

## 🔹 1️⃣6️⃣ `androidx-constraintlayout`

কাজ কী করে:

 Advanced & performance-friendly UI layout
 Flat view hierarchy

কখন লাগে:

 Complex UI layout বানাতে

---

#  Big Picture (এক লাইনে)

 এই dependencies মিলেই তৈরি হয়:

 UI (Material + ConstraintLayout)
 Architecture (ViewModel + LiveData)
 Storage (DataStore)
 Security
 Testing
 Performance & Compatibility

---

##  Beginner Android App-এ MUST dependencies

```text
core-ktx
appcompat
material
activity
constraintlayout
lifecycle-viewmodel-ktx
```

